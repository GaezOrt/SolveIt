<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="gunner.gunner">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permisssion.ACCESS_COARSE_LOCATION" />
    <application
        android:launchMode = "singleTop"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="ServPro"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ConfirmationCode"></activity>
        <activity android:name=".WelcomeWindow" android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".addComment"></activity>
        <activity android:name=".FindInDatabase"></activity>
        <activity android:name=".ProfileUser"></activity>
        <activity android:name=".SignUp"
            android:taskAffinity="com.some.new" >

        </activity>
        <activity android:name=".LogIn"></activity>
        <activity android:name=".Electricidad">
        </activity>
        <activity android:name=".MainActivity">

        </activity>
        <service android:name=".LogInService"></service>
        <service
            android:name=".DownloadStuffInBackground"
            android:exported="false"/>
<service android:name=".SignUpService"></service>
        <service android:name=".KeepEverythingUpdated"></service>
            </application>

</manifest>